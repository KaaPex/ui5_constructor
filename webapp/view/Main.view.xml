<mvc:View controllerName="evola.controller.Main"
          xmlns="sap.m"
          xmlns:core="sap.ui.core"
          xmlns:mvc="sap.ui.core.mvc"
          xmlns:evola="evola.ui.containers"
          xmlns:form="sap.ui.layout.form"
          height="100%">
    <Page id="MainPageId"
          title="{i18n>title}"
          enableScrolling="false"
          showHeader="false"
          showNavButton="true"
          class="sapUiSizeCompact">

        <content>
            <Label text="Product" labelFor="productInput"/>
            <Input id="productInput" value="test"/>
            <form:SimpleForm editable="true"
                             layout="ColumnLayout"
                             singleContainerFullSize="false"
                             class="noTitle">
                <form:content>
                    <Label text="Product" labelFor="productInput2"/>
                    <Input id="productInput2" value="test"/>
                    <!--<evola:Constructor items="{/Controls}">-->
                        <!--<evola:items>-->
                            <!--<evola:ConstructorItem-->
                                    <!--type="{type}"-->
                                    <!--value="{value}"-->
                                    <!--change="onChangeValue"-->
                                    <!--liveChange="onLiveChange">-->
                                <!--&lt;!&ndash;<evola:ConstructorLabel text="{label}" forId="{id}" visible="{labelEnabled}"/>&ndash;&gt;-->
                                <!--&lt;!&ndash;<evola:ConstructorController text="{label}" forId="{id}" visible="{labelEnabled}"/>&ndash;&gt;-->
                            <!--</evola:ConstructorItem>-->
                        <!--</evola:items>-->
                    <!--</evola:Constructor>-->
                </form:content>
            </form:SimpleForm>

            <evola:Constructor
                    layout="ColumnLayout"
                    items="{/Controls}">
                <evola:items>
                    <evola:ConstructorItem
                            name="{id}"
                            label="{label}"
                            type="{type}"
                            value="{value}"
                            change="onChangeValue"
                            liveChange="onLiveChange">
                        <!--<evola:ConstructorLabel text="{label}" forId="{id}" visible="{labelEnabled}"/>-->
                        <!--<evola:ConstructorController text="{label}" forId="{id}" visible="{labelEnabled}"/>-->
                    </evola:ConstructorItem>
                </evola:items>
            </evola:Constructor>
        </content>
    </Page>
</mvc:View>